<x-layouts.site :title="__('Privacy Policy')">
    <div class="mx-auto max-w-3xl px-4 py-10 sm:px-6 lg:px-8 text-gray-700 dark:text-gray-300">
        <h1 class="text-2xl font-bold text-gray-900 dark:text-gray-100">{{ __('Privacy Policy') }} ({{ __('Datenschutzerklärung') }})</h1>
        <p class="mt-2 text-sm italic text-gray-500 dark:text-gray-400">{{ __('Draft template — have this reviewed by a lawyer familiar with GDPR before launch.') }}</p>

        <h2 class="mt-6 text-lg font-semibold text-gray-900 dark:text-gray-100">{{ __('Data we collect') }}</h2>
        <p class="mt-2">{{ __('Account data (name, email), and click-tracking data used to attribute referrals to partner stores for our internal reporting.') }}</p>

        <h2 class="mt-6 text-lg font-semibold text-gray-900 dark:text-gray-100">{{ __('Cookies and tracking') }}</h2>
        <p class="mt-2">{{ __('We use essential cookies for authentication and language, and, based on your consent, click-tracking cookies to measure which offers are used.') }}</p>

        <h2 class="mt-6 text-lg font-semibold text-gray-900 dark:text-gray-100">{{ __('Your rights') }}</h2>
        <p class="mt-2">{{ __('Under GDPR you may request access, correction, or deletion of your personal data at any time from your profile page.') }}</p>

        <h2 class="mt-6 text-lg font-semibold text-gray-900 dark:text-gray-100">{{ __('Contact') }}</h2>
        <p class="mt-2">[data-protection@example.com]</p>
    </div>
</x-layouts.site>
